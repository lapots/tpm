package com.lapots.breed.platform.tpm.core.api.installer;

import com.lapots.breed.platform.tpm.core.artifact.consistency.Artifact;

public interface Installer {
    void install(Artifact artifact);
}
